---
created_at: 2026-06-07
updated_at: 2026-06-07
created_by: claude (opus-4.8)
modified_by: claude (opus-4.8)
---

# Formy02 — Implementation Plan V03

Planner: claude (opus-4.8). Source requirements:
[20260607_FORMY02_PRD.md](20260607_FORMY02_PRD.md). V03 supersedes
[20260607_FORMY02_PLAN_V02.md](20260607_FORMY02_PLAN_V02.md), incorporating the
single concern raised in
[20260607_FORMY02_PLAN_V02_ASSESSMENT_CODEX.md](20260607_FORMY02_PLAN_V02_ASSESSMENT_CODEX.md):
the theme precedence algorithm in V02 §7 is logically contradictory — it
prioritizes `?theme=` on every page load, which would allow a later GoLightly
click to override a Formy02 preference the user already set via the in-app toggle.
V03 replaces §7 with a correct four-step algorithm that uses a `formy.themeUserSet`
marker to distinguish a user-set preference from a GoLightly-provided initial hint.
All other V02 sections are retained without material change.

## 1. Strategy and scope

Formy02 is a re-skin + datastore swap of the existing Formy app, not a rewrite.
The plan is to **port Formy's Python backend into this repo essentially intact**,
then make four scoped changes on top of it:

1. Replace SQLite with PostgreSQL at the engine/config layer.
2. Replace the Bootstrap-based Jinja templates + `static/style.css` with a
   Tailwind build that reproduces GoLightly04's design tokens and components.
3. Add a theme system (light/dark) with a pre-paint hand-off from a `?theme=`
   URL parameter, plus an in-app toggle, governed by the precedence algorithm
   in §7.
4. Give the public questionnaire a playful hero with a waving-photo placeholder.

Backend route signatures, models, auth, email, audio, logging, and error
handling are preserved in behavior; most edits are in templates, config, the DB
engine, and a small amount of new theme/static plumbing.

A fifth, independent change lives in the **GoLightly04 repo** (forward theme on
the footer link) and is described in §11. It is small and isolated.

## 2. Technology

- Language/runtime: Python 3.13 in a project-local `venv` (per `AGENTS.md`).
- Web: FastAPI + Jinja2 (`Jinja2Templates`), Uvicorn, server-rendered HTML.
- ORM: SQLModel / SQLAlchemy.
- Database: PostgreSQL via the `psycopg` (v3) driver.
- Styling: Tailwind CSS built to a static stylesheet served from
  `src/app/static/`. Recommended: the standalone Tailwind CLI so no Node app is
  introduced; a `tailwind.config.js` + input CSS hold the ported tokens.
- Auth: bcrypt password hashing, `python-jose` JWT, `itsdangerous` signed
  session cookies (all already in Formy's `requirements.txt`).
- Email: Gmail SMTP (existing `utilities/email.py`).
- Logging: Loguru per `docs/LOGGING_PYTHON_V07.md`.
- Backup: `pg_dump` / `pg_restore` invoked via `subprocess.run` with an
  argument list (no `shell=True`).

Dependencies added vs. Formy: `psycopg[binary]` (Postgres driver). Dependency
removed: implicit SQLite usage. Tailwind is a build-time tool, not a Python dep.

## 3. Project structure

Mirror Formy's layout (keeps the ≤3-subdirectory convention from the PRD):

```
Formy02/
├── src/app/
│   ├── main.py                 # FastAPI entry (lifespan, static, templates, routers)
│   ├── models/                 # database.py + SQLModel tables (unchanged shape)
│   ├── routes/                 # auth.py, admin.py, questionnaire.py
│   ├── utilities/              # config, logging_setup, error_handlers, auth,
│   │                           #   email, audio, db_backup, on_start_up
│   ├── templates/              # restyled Jinja templates (+ theme partials)
│   └── static/                 # tailwind build output, images/ (waving placeholder)
├── tailwind.config.js          # ported GoLightly tokens
├── src/styles/tailwind.css     # @tailwind directives + ported component layer
├── requirements.txt
├── .env / .env.example
└── docs/
```

## 4. Data layer (SQLite → PostgreSQL)

Reference: `Formy/src/app/models/database.py`, `on_start_up.py`.

### 4.1 Engine and session

`models/database.py`: replace the SQLite `DATABASE_URL` and
`connect_args={"check_same_thread": False}` with a Postgres URL assembled from
config (`postgresql+psycopg://user:pass@host:port/dbname`). Keep `create_engine`,
`create_db_and_tables()` (still `SQLModel.metadata.create_all`), and
`get_session()` as the FastAPI dependency.

### 4.2 Question.options — concrete JSONB mapping

**The problem fixed in V02:** Formy's `Question` model stores options via a
`options_json: Optional[str]` field backed by a `Column("options", Text)`, plus
a Python `@property` / `@setter` pair that calls `json.dumps` / `json.loads`.
V01 said "map to JSON/ARRAY" without naming a design; the Codex assessment
correctly identified that an implementer could leave the SQLite Text encoding in
place or accidentally collide with the existing property.

**The concrete mapping:** Replace the `Text`-backed `options_json` field and the
`@property` / `@setter` pair with a single mapped field using
`sqlalchemy.dialects.postgresql.JSONB`:

```python
from sqlalchemy.dialects.postgresql import JSONB

class Question(SQLModel, table=True):
    ...
    options: Optional[List[str]] = Field(
        default=None,
        sa_column=Column("options", JSONB),
    )
```

The DB column name stays `"options"` — the same name Formy uses — so there is
no migration rename. The `JSONB` type instructs SQLAlchemy to handle
serialization natively; no `json.dumps` / `json.loads` call is needed in
application code. The backing private field `options_json` and its property pair
are removed.

**Route and template contracts preserved without modification:**

- `routes/admin.py` create question (line 108): `q.options = parsed_options`
  — assigns a `list[str] | None` to the mapped field; works as-is.
- `routes/admin.py` update question (line 142): `q.options = [...]`
  — same assignment; works as-is.
- `templates/questionnaire.html` multiple-choice rendering: iterates `q.options`
  — receives a `list[str]` from the ORM; works as-is.
- `routes/admin.py` update question JSON path (line 139): reads
  `body["options"]` and assigns to `q.options`; works as-is because the ORM
  field accepts a Python list directly.

No changes to admin.py or questionnaire.html are required for options handling.
The only change is in `models/question.py`: swap the column type, drop the
private field, and remove the property/setter.

### 4.3 Startup flow — admin seeding removed

**The problem fixed in V02:** Formy's `on_start_up.py:create_admin_user()`
creates missing whitelisted users with `password=hash_password("test")`,
`is_admin=True`, and `email_verified=True`, and also auto-verifies any existing
unverified admin. If carried forward, Formy02 could launch with automatically
verified admin accounts protected by a known password, bypassing the very
registration and verification flow the PRD requires.

**The fix:** Remove `create_admin_user()` entirely. `on_start_up.py` contains
only `check_database()`, which calls `create_db_and_tables()`. The lifespan in
`main.py` calls only `configure_logging()` and `check_database()`.

The `ALLOWED_ADMIN_EMAILS` environment variable is retained — it continues to
gate registration in `routes/auth.py` (the whitelist check that returns 403 for
non-whitelisted addresses). It no longer drives any auto-seeding.

**First-deploy bootstrap:** An admin registers at `/register` with a
whitelisted email address, receives the verification email (requires SMTP to be
configured), clicks the link at `{URL_BASE_WEBSITE}/verify?token=<jwt>`, and
can then log in. This is the correct flow the PRD describes and the only
supported path.

If SMTP is unavailable during initial setup (a known operational edge case), the
operator must configure SMTP before first admin registration — not work around
it with startup seeding. The README documents this constraint explicitly.

### 4.4 Remaining schema

Schema shape is unchanged (PRD §8.2): `User`, `SurveyedPerson`, `Question`,
`Response`, `ContractSurveyedPersonQuestionResponse`. This is a fresh build
with no SQLite data to migrate; `create_db_and_tables()` on a clean Postgres
database creates all tables. Audio bytes remain on the filesystem under
`AUDIO_FILES_PATH`; only paths are stored. No change to `utilities/audio.py`.

### 4.5 Startup health check

For Postgres, `check_database()` should issue a lightweight connectivity check
(e.g. `SELECT 1`) before `create_all`, and log a fatal error and raise if the
database is unreachable — consistent with the `docs/LOGGING_PYTHON_V07.md`
fail-fast posture. An unreachable database must not silently allow the app to
start and serve 500s.

## 5. Configuration and environment

Reference: `Formy/src/app/utilities/config.py`.

- Remove the SQLite keys `NAME_DB` / `PATH_DATABASE`. Add Postgres keys:
  `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, and optionally
  `DB_SCHEMA`. These are required in all environments via `_require`.
- Rename logging size var to `LOG_MAX_SIZE_IN_MB` (Formy used `LOG_MAX_SIZE`).
- Keep all auth/email keys, `URL_BASE_WEBSITE`, `AUDIO_FILES_PATH`,
  `PATH_PROJECT_RESOURCES`, `NAME_APP`, `RUN_ENVIRONMENT`.
- The backup utility also reads the Postgres env vars when shelling out to
  `pg_dump`/`pg_restore`; it does not maintain its own DB connection string.
- Update `.env.example` accordingly; document every key in the README.

## 6. Styling system (Tailwind port of GoLightly04)

Reference: `GoLightly04/web/tailwind.config.js`,
`GoLightly04/web/src/styles/globals.css`.

- `tailwind.config.js`: copy the `primary` / `secondary` / `accent` / `calm`
  palettes, the semantic `canvas`/`raised`/`inset`/`overlay`/`subtle`/`strong`/
  `ink`/`ink-muted` colors (driven by CSS variables), the `display`/`sans` font
  families, `maxWidth.app`, and the gentle keyframe/animation. Set
  `darkMode: 'class'`. Point `content` at `src/app/templates/**/*.html`.
- Input CSS (`src/styles/tailwind.css`): `@tailwind base/components/utilities`,
  the `:root` and `.dark` CSS-variable blocks from globals.css, and the
  `@layer components` classes `.btn-primary`, `.btn-secondary`, `.btn-outline`,
  `.input-field`, `.card`, `.section-heading`.
- Build step: Tailwind CLI compiles to `src/app/static/app.css`, referenced from
  `base.html`. Document the build command in the README. FastAPI's existing
  `/static` mount serves it unchanged.
- Templates: rewrite `base.html` to load the Tailwind stylesheet and define the
  GoLightly-style shell (sticky `backdrop-blur` header with brand + theme toggle,
  gradient page background, minimal footer). Convert each page template from
  Bootstrap classes to the Tailwind/component classes. Templates affected:
  `base.html`, `questionnaire.html`, `thank_you.html`, `login.html`,
  `register.html`, `forgot_password.html`, `reset_password.html`,
  `email_reset_password.html`, `admin/index.html`, `admin/questions.html`,
  `admin/responses.html`.

## 7. Theme system and hand-off

Reference: `GoLightly04/web/src/components/ThemeToggle.tsx` (behavior to
mirror), `GoLightly04/web/src/components/AppShell.tsx` (footer link to modify).

### 7.1 The core design requirement

The PRD (§7.2) states two rules that must hold simultaneously:

- The `?theme=` URL parameter is **an initial hint only** — GoLightly hands off
  the theme it is currently displaying so the visitor's experience feels
  continuous when they follow the footer link.
- Once the user **toggles in Formy02**, their Formy02 preference takes
  precedence over the incoming parameter.

V02's algorithm (check `?theme=` first, then fall back to `formy.theme`) violates
the second rule: any subsequent navigation to Formy02 from GoLightly with
`?theme=dark` or `?theme=light` would override a preference the user already
expressed by toggling. V03 fixes this with a two-key localStorage design.

### 7.2 Storage keys

Two `localStorage` keys govern theme state in Formy02:

| Key | Type | Meaning |
|-----|------|---------|
| `formy.theme` | `"light"` \| `"dark"` | The theme currently applied in this browser. Written by the pre-paint script (when accepting a GoLightly hint) and by the in-app toggle. |
| `formy.themeUserSet` | `"true"` | Set **only** when the user explicitly toggles via the Formy02 in-app button. Absent (or any other value) means no explicit local choice has been recorded. |

The `formy.themeUserSet` flag is the critical invariant: its presence means the
user's voice wins over any incoming URL parameter.

### 7.3 Pre-paint precedence algorithm

An inline `<script>` block in `base.html <head>` runs synchronously before the
body renders, preventing a theme flash. The algorithm in execution order:

1. **User-set preference wins.** If `formy.themeUserSet === "true"` and
   `formy.theme` is exactly `"light"` or `"dark"`, apply `formy.theme` and
   ignore `?theme=` entirely.
2. **GoLightly initial hint.** Else if `?theme=` is exactly `"light"` or
   `"dark"`, apply that value and write it to `formy.theme` (so subsequent
   navigation without a param stays consistent). Do **not** write
   `formy.themeUserSet`; the GoLightly hint is not a user-set preference.
3. **Previously stored hint.** Else if `formy.theme` is exactly `"light"` or
   `"dark"`, apply it.
4. **Cold default.** Apply `"light"`.

Applying a theme means: toggle the `dark` class on `document.documentElement`
and set `document.documentElement.style.colorScheme` to the resolved value.
Any malformed `?theme=` value (anything other than `"light"` or `"dark"`) is
silently discarded and the algorithm falls through to step 3 or 4 — the page
never breaks.

Pseudocode rendering of the pre-paint script:

```javascript
(function () {
  var stored = localStorage.getItem('formy.theme');
  var userSet = localStorage.getItem('formy.themeUserSet') === 'true';
  var param = new URLSearchParams(location.search).get('theme');
  var valid = function (v) { return v === 'light' || v === 'dark'; };

  var theme;
  if (userSet && valid(stored)) {
    // Step 1 — user explicitly toggled in Formy02; ignore incoming param
    theme = stored;
  } else if (valid(param)) {
    // Step 2 — GoLightly initial hint
    theme = param;
    localStorage.setItem('formy.theme', theme);
  } else if (valid(stored)) {
    // Step 3 — previously stored hint
    theme = stored;
  } else {
    // Step 4 — cold default
    theme = 'light';
  }

  document.documentElement.classList.toggle('dark', theme === 'dark');
  document.documentElement.style.colorScheme = theme;
}());
```

### 7.4 In-app toggle

The theme toggle button in `base.html` calls a small inline handler on click.
The handler:

1. Reads the current theme from `document.documentElement.classList.contains('dark')`.
2. Computes the next theme (`dark` → `light`, `light` → `dark`).
3. Toggles the `dark` class and sets `style.colorScheme`.
4. Writes `formy.theme = <next>` to `localStorage`.
5. Writes `formy.themeUserSet = "true"` to `localStorage`.

Step 5 is what distinguishes a user action from a GoLightly hint. Once
`formy.themeUserSet` is `"true"`, every future page load in this browser takes
step 1 of the pre-paint algorithm and the `?theme=` parameter is ignored.

The toggle mirrors GoLightly's `ThemeToggle.tsx` behavior (flip `.dark`, update
`color-scheme`, persist to storage) while adding the `formy.themeUserSet` write.

### 7.5 Server-side role

The server does not need to read `?theme=` or any theme cookie. Theme resolution
is entirely client-side: the pre-paint inline script runs before CSS is applied,
eliminating the need for a server-set class. No route changes are required for
theme support.

The `?theme=` parameter flows harmlessly through all routes (FastAPI ignores
unrecognized query params) — no server filtering or validation is needed.

### 7.6 No-JS behavior

Visitors with JavaScript disabled receive the default light theme. This is
acceptable for this deployment; the admin audience uses the app authenticated
and in a controlled browser environment. A server-read cookie fallback is noted
as a future enhancement but is not required for the first implementation (see
§13 open decisions).

## 8. Page flows (behavior preserved, presentation restyled)

Reference: `Formy/src/app/routes/`.

- Public questionnaire (`GET /` → `questionnaire_page`): same query for
  `is_active` questions ordered by id; template gains a playful hero with the
  waving-photo placeholder (`static/images/waving_placeholder.png`) and warm,
  anonymity-reassuring copy. Question rendering (yes/no radios, multiple-choice
  radios, text area) and the per-question MediaRecorder audio widget are
  preserved, re-skinned to the new component classes.
- Submit (`POST /submit` → `submit_questionnaire`): unchanged logic — create
  `SurveyedPerson`, persist only non-blank/with-audio responses, link via
  junction, save audio with the zero-padded dated filename, redirect to
  `/thank-you`.
- Thank-you (`GET /thank-you`): restyled, with a friendly return path.
- Auth (`routes/auth.py`): register (whitelist check), send verification email,
  `GET /verify`, login/logout, forgot/reset password — all preserved; only
  templates change.
- Admin Questions (`admin/questions.html`): searchable/sortable questions table
  + create form restyled as GoLightly cards. Client-side search/sort preserved.
- Admin Responses (`admin/responses.html`): table grouped by `SurveyedPerson`
  with the Questions-count column opening a modal of answers + audio playback,
  restyled with GoLightly's overlay/`backdrop-blur` treatment.

## 9. Logging and error handling

- `utilities/logging_setup.py`: align to `docs/LOGGING_PYTHON_V07.md` — single
  daily-or-size rotating file sink, env-driven sinks/levels, `enqueue` in
  testing/production, `sys.excepthook` for uncaught exceptions, early-exit logs,
  `LOG_MAX_SIZE_IN_MB` naming. Invoked first in the lifespan.
- `utilities/error_handlers.py`: keep the three handlers (HTTP, validation,
  unhandled). JSON error bodies follow `docs/ERROR_REQUIREMENTS.md`
  (`{ error: { code, message, details?, status } }`, sanitized in production);
  HTML routes render friendly themed error pages while logging full detail.

## 10. Backup utility — admin-facing Postgres design

**The problem fixed in V02:** V01 said to adapt `db_backup.py` to
`pg_dump`-based dumps and left "exact CLI invocation/scheduling" as a TODO-phase
detail. Formy's backup utility is not just an operator-side script: the admin
dashboard exposes create, restore, list, download, and delete actions through
routes and UI — all of which the PRD requires to be preserved. The old
CSV-in-zip approach is SQLite-specific (the restore code includes a literal
`UPDATE sqlite_sequence` call) and cannot be carried forward.

### 10.1 Admin routes preserved (signatures unchanged)

The following route surface from `Formy/src/app/routes/admin.py` is retained
without signature changes:

| Method   | Path                          | Purpose                     |
|----------|-------------------------------|-----------------------------|
| `GET`    | `/admin`                      | Dashboard — lists backups   |
| `POST`   | `/admin/db/backup`            | Create a new backup         |
| `POST`   | `/admin/db/restore`           | Restore from uploaded file  |
| `GET`    | `/admin/db/backups/{filename}`| Download a backup           |
| `DELETE` | `/admin/db/backups/{filename}`| Delete a backup             |

Route handlers call into `utilities/db_backup.py` functions: `create_backup()`,
`restore_backup()`, `list_backups()`, `get_backup_path()`, `delete_backup()` —
same call sites as Formy. The JSON response shapes (`{"ok": True, "filename":
...}`) are preserved so the admin dashboard JavaScript requires no changes
beyond the file-extension references described below.

### 10.2 Backup artifact format

Backups are produced by `pg_dump` in **custom format** (`-Fc`). Custom format
is binary, internally compressed, and directly consumed by `pg_restore` — it is
the appropriate choice for an in-app backup/restore cycle on a single-server
deployment.

Naming convention: `backup_YYYYMMDD_HHMMSS.dump` (`.dump` replaces Formy's
`.zip`). Files are stored in `{PATH_PROJECT_RESOURCES}/backups_db/`, the same
directory Formy uses, created with `mkdir -p` on first use.

`list_backups()` globs `backup_*.dump` and returns `{name, size_kb, modified}`
dicts exactly as before; the dashboard table is unchanged.

### 10.3 create_backup() implementation

`db_backup.create_backup()` invokes `pg_dump` via `subprocess.run` with an
explicit argument list (no `shell=True`). The DB connection parameters — host,
port, database name, and user — are drawn from the same config env vars used by
the ORM. The password is passed to the subprocess environment as `PGPASSWORD`
(standard Postgres mechanism) so it never appears in a command string or log.

```
pg_dump \
  --format=custom \
  --no-owner \
  --no-privileges \
  --file={backups_dir}/backup_{timestamp}.dump \
  --host={DB_HOST} --port={DB_PORT} \
  --username={DB_USER} {DB_NAME}
```

The function raises `RuntimeError` if the subprocess exits non-zero; the route
catches `RuntimeError` and returns HTTP 500, consistent with Formy's existing
pattern.

### 10.4 restore_backup() implementation

The admin uploads a `.dump` file. The route handler reads the upload bytes and
passes them to `db_backup.restore_backup(dump_bytes: bytes)`.

`restore_backup()` writes the bytes to a named temporary file (via `tempfile`),
then runs `pg_restore` via `subprocess.run` with an explicit argument list:

```
pg_restore \
  --clean \
  --if-exists \
  --no-owner \
  --no-privileges \
  --format=custom \
  --host={DB_HOST} --port={DB_PORT} \
  --username={DB_USER} \
  --dbname={DB_NAME} \
  {tempfile_path}
```

The `--clean --if-exists` flags drop and recreate objects before restoring,
which is the Postgres equivalent of Formy's truncate-then-insert flow. The
temporary file is deleted in a `finally` block regardless of success or failure.

Because `pg_restore` operates against the live database, active connections
from the FastAPI app are not automatically terminated. For a single-admin,
low-traffic deployment this is acceptable; the plan notes that a pre-restore
`SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname = $1 AND
pid <> pg_backend_pid()` step can be added if needed, but is not required for
the first implementation.

### 10.5 Admin UI changes

The dashboard template (`admin/index.html`) requires two small changes:

- The restore file input changes `accept=".zip"` to `accept=".dump"`.
- The descriptive text changes from "CSV zip archive" to "pg_dump custom-format
  archive (.dump)".

The JavaScript functions (`createBackup`, `confirmRestore`, `doRestore`,
`deleteBackup`) are otherwise unchanged — they operate on filenames returned by
the API, so the `.dump` extension flows through without modification.

The existing restore warning — "This will erase all current data" — is
preserved. No additional UI confirmation step is needed; the existing modal
already gates the action.

### 10.6 Security boundaries

- `get_backup_path(filename)` retains Formy's path-traversal guard: it resolves
  the joined path and asserts it starts with the resolved `backups_dir` before
  returning the `Path` object. The guard applies to download, delete, and restore
  operations.
- All five backup/restore routes require `Depends(require_admin)`.
- `pg_dump`/`pg_restore` are invoked with an explicit argument list, never via
  `shell=True`. The `PGPASSWORD` env var is set on the subprocess environment
  dict, not interpolated into a command string.

## 11. GoLightly04-side change (separate repo)

Reference: `GoLightly04/web/src/components/AppShell.tsx` (footer "Help Us
Improve" anchor → `https://formy.go-lightly.love`), `ThemeToggle.tsx`
(`golightly.theme` in `localStorage`, `.dark` on `<html>`).

The footer anchor is currently a static `href="https://formy.go-lightly.love"`.
The change: compute the `href` to include the current theme at click time by
reading `localStorage.getItem('golightly.theme')` (or
`document.documentElement.classList.contains('dark')` as the fallback) and
appending `?theme=dark` or `?theme=light`. The anchor becomes dynamic — either
via a React `onClick` handler that rewrites `location.href`, or by converting
the `<a>` to render with a computed `href` derived from the current theme state.

This ensures a visitor on GoLightly04 in dark mode follows a link like
`https://formy.go-lightly.love?theme=dark` and lands in Formy02's dark theme
on their first visit, satisfying the cross-site continuity goal without sharing
`localStorage` across origins.

No other GoLightly changes. Isolated, low risk; testable independently.
Implement as the final step so Formy02's `?theme=` handling already exists to
receive it.

## 12. Documentation deliverables

- README per the project's format conventions (setup, env, Tailwind build,
  first-admin registration procedure, run).
- API docs via `docs/CREATE_API_DOCS.md` for the HTTP surface.
- Database docs: adapt `docs/CREATE_DATABASE_DOCS.md` in spirit (a
  `TABLE_REFERENCE` + Mac/Ubuntu setup + usage) since that skill targets a
  TS/Sequelize monorepo and must be reinterpreted for a Python/SQLAlchemy single
  app. Flagged as a known conflict (PRD §15.2).
- All generated `.md` files carry the four-key frontmatter; commits follow
  `AGENTS.md`.

## 13. Sequencing, risks, and open decisions

Suggested build order: (1) port backend + Postgres engine/config + JSONB options
field; (2) Tailwind build + `base.html` shell + theme system (§7); (3) restyle
questionnaire/thank-you + waving hero; (4) restyle auth + admin pages including
backup dashboard; (5) logging/errors alignment; (6) backup utility (`pg_dump`
implementation); (7) docs; (8) GoLightly footer change.

Resolved from V01 (carried forward from V02):

- `Question.options` Postgres mapping: **JSONB column** (§4.2).
- Startup admin seeding: **removed entirely** (§4.3). First admin registers
  through the whitelisted registration flow.
- Backup/restore surface: **admin-facing `pg_dump` custom-format design** with
  all five routes preserved (§10).

Resolved from V02:

- Theme precedence algorithm: **four-step algorithm with `formy.themeUserSet`
  marker** (§7). A user-set Formy02 preference wins over any future incoming
  `?theme=` parameter; the GoLightly hint is accepted only when no user
  preference has been recorded. The contradiction in V02 §7 is eliminated.

Remaining open decisions:

- **Tailwind tooling:** standalone Tailwind CLI vs. minimal Node/PostCSS.
  Recommended: standalone CLI to avoid introducing a Node project.
- **No-JS cookie fallback for theme:** confirm whether the default light theme
  for JS-disabled visitors is acceptable, or add a server-read cookie as a
  fallback. Not required for the first implementation.
- **`CREATE_DATABASE_DOCS.md` adaptation depth** for a non-monorepo Python app.
- **Seed questions for first deploy** (PRD §15.5): none required by the PRD;
  confirm before implementation.
- **Production domain and Ubuntu service runbook** (PRD §15.6): carry forward
  `Formy/docs/UBUNTU_RUN_SERVICE.md` and adapt for Postgres service dependency.
